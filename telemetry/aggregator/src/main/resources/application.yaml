spring:
  application:
    name: aggregator
  config:
    import: configserver:http://localhost:8080
  cloud:
    config:
      fail-fast: true
      retry:
        use-random-policy: true
        max-interval: 6000

